{
  "name": "@ai-booth-observer/worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API proxy for AI Booth Observer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-booth-observer/shared": "workspace:*",
    "@anthropic-ai/sdk": "^0.68.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.46.0"
  }
}
